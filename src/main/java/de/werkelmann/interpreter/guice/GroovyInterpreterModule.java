package de.werkelmann.interpreter.guice;

import com.google.inject.AbstractModule;

public class GroovyInterpreterModule extends AbstractModule {

	@Override
	protected void configure() {
		// bind(Parser.class).to(GroovyParser.class);
	}

}
